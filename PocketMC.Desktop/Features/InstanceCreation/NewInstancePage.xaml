<Page x:Class="PocketMC.Desktop.Features.InstanceCreation.NewInstancePage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="New Instance">

    <Grid Margin="28,20,28,24" VerticalAlignment="Stretch">
        <DockPanel LastChildFill="True">

        <Grid DockPanel.Dock="Top"
              Width="{Binding ElementName=ContentLayoutRoot, Path=ActualWidth}"
              MaxWidth="980"
              HorizontalAlignment="Left"
              Margin="0,0,0,20">
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Left">
                <ui:Button Content="Back"
                           Appearance="Secondary"
                           Icon="{ui:SymbolIcon ArrowLeft24}"
                           MinWidth="88"
                           Margin="0,0,14,0"
                           Click="BtnBack_Click"/>

                <ui:TextBlock FontTypography="Title"
                              Text="Create a New Instance"
                              Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                              VerticalAlignment="Center"/>
            </StackPanel>

        </Grid>

        <!-- Footer Action Bar -->
        <Border DockPanel.Dock="Bottom"
                Margin="0,16,0,0"
                Padding="20,16"
                Background="{DynamicResource CardBackgroundFillColorDefaultBrush}"
                BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                BorderThickness="1"
                CornerRadius="12">
            <Grid HorizontalAlignment="Stretch">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0"
                    x:Name="ErrorCallout"
                    HorizontalAlignment="Left"
                    Visibility="Collapsed"
                    Background="#26F44336"
                    BorderBrush="#66F44336"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="12"
                    Margin="0,0,16,0">
                <StackPanel Orientation="Horizontal">
                    <Border Width="8"
                            Height="8"
                            Background="#FFB4AB"
                            CornerRadius="4"
                            Margin="0,5,10,0"
                            VerticalAlignment="Top"/>
                    <TextBlock x:Name="TxtError"
                               Foreground="#FFE8E6"
                               TextWrapping="Wrap"
                           VerticalAlignment="Center"/>
                </StackPanel>
            </Border>

            <StackPanel Grid.Column="1"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right">
                <ui:Button x:Name="BtnCancel"
                           Content="Cancel"
                           Appearance="Secondary"
                           Margin="0,0,8,0"
                           Click="BtnBack_Click"/>
                <ui:Button x:Name="BtnCreate"
                           Content="Create and Download"
                           Appearance="Primary"
                           Icon="{ui:SymbolIcon ArrowDownload24}"
                           Click="BtnCreate_Click"/>
            </StackPanel>
            </Grid>
        </Border>

        <Grid>
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <StackPanel x:Name="ContentLayoutRoot" MaxWidth="980" HorizontalAlignment="Left">

            <ui:Card x:Name="FormCard"
                     Padding="18"
                     Margin="0">
                <StackPanel x:Name="InputsPanel">
                    <TextBlock Text="Basics"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,20"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="24" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>

                        <!-- Left Column: Name and Description -->
                        <StackPanel Grid.Column="0">
                            <TextBlock Text="Server Name"
                                       Margin="0,0,0,6"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <ui:TextBox x:Name="TxtName"
                                        PlaceholderText="My Cool Server"
                                        Margin="0,0,0,16"/>

                            <TextBlock Text="Description"
                                       Margin="0,0,0,6"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <ui:TextBox x:Name="TxtDescription"
                                        PlaceholderText="What this instance is for"
                                        Margin="0,0,0,0"/>
                        </StackPanel>

                        <!-- Right Column: Types and Versions -->
                        <StackPanel Grid.Column="2">
                            <TextBlock Text="Server Type"
                                       Margin="0,0,0,6"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <ComboBox x:Name="CmbServerType"
                                      Margin="0,0,0,16"
                                      HorizontalAlignment="Stretch"
                                      MinHeight="40"
                                      SelectionChanged="CmbServerType_SelectionChanged">
                                <ComboBoxItem Content="Vanilla" IsSelected="True"/>
                                <ComboBoxItem Content="Paper"/>
                                <ComboBoxItem Content="Fabric"/>
                                <ComboBoxItem Content="Forge"/>
                                <ComboBoxItem Content="NeoForge"/>
                                <ComboBoxItem Content="Bedrock (BDS)"/>
                                <ComboBoxItem Content="Pocketmine (PHP)"/>
                            </ComboBox>

                            <TextBlock Text="Minecraft Version"
                                       Margin="0,0,0,6"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <ComboBox x:Name="CmbVersion"
                                      Margin="0,0,0,8"
                                      HorizontalAlignment="Stretch"
                                      MinHeight="40"
                                      DisplayMemberPath="Id"
                                      SelectionChanged="CmbVersion_SelectionChanged"/>
                            
                            <TextBlock x:Name="TxtVersionState"
                                       Text="Choose a server type to load available versions."
                                       FontSize="11"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap"
                                       Margin="0,0,0,0"/>
                        </StackPanel>

                        <!-- Second Row for Options/Loaders -->
                        <StackPanel Grid.Row="1" Grid.Column="0" Margin="0,16,0,0">
                            <CheckBox x:Name="ChkShowSnapshots"
                                      Content="Show snapshots and pre-releases"
                                      Checked="ChkShowSnapshots_Changed"
                                      Unchecked="ChkShowSnapshots_Changed"/>
                             <TextBlock x:Name="TxtForgeWarning"
                                       Visibility="Collapsed"
                                       Foreground="#F9E2AF"
                                       FontSize="12"
                                       TextWrapping="Wrap"
                                       Margin="0,8,0,0"/>
                        </StackPanel>

                        <StackPanel x:Name="LoaderVersionPanel" Grid.Row="1" Grid.Column="2" Visibility="Collapsed" Margin="0,16,0,0">
                            <TextBlock Text="Loader Version"
                                       Margin="0,0,0,6"
                                       Foreground="{DynamicResource TextFillColorPrimaryBrush}"/>
                            <ComboBox x:Name="CmbLoaderVersion"
                                      HorizontalAlignment="Stretch"
                                      MinHeight="40"
                                      DisplayMemberPath="Version"/>
                        </StackPanel>
                    </Grid>

                    <!-- Addons / Plugins -->
                    <StackPanel x:Name="AddonPanel" Margin="0,16,0,0">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto" />
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>
                            <CheckBox x:Name="ChkEnableGeyser"
                                      Grid.Column="0"
                                      VerticalAlignment="Top"
                                      Margin="0,2,12,0"/>
                            
                            <StackPanel Grid.Column="1">
                                <TextBlock Text="Enable Cross-play (Geyser + Floodgate)"
                                           FontWeight="Medium"
                                           FontSize="13"
                                           Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                                           Margin="0,0,0,4"/>
                                <TextBlock FontSize="12"
                                           Opacity="0.8"
                                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                           TextWrapping="Wrap">
                                    <Run Text="Allows Minecraft Bedrock players to join this Java server."/>
                                </TextBlock>
                            </StackPanel>
                        </Grid>
                    </StackPanel>

                    <Border Height="1" Background="{DynamicResource CardStrokeColorDefaultBrush}" Margin="0,24,0,24"/>

                    <TextBlock Text="Compliance"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                               Margin="0,0,0,16"/>

                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>
                        <CheckBox x:Name="ChkAcceptEula"
                                  Grid.Column="0"
                                  VerticalAlignment="Top"
                                  Margin="0,2,12,0"
                                  Checked="ChkAcceptEula_Changed"
                                  Unchecked="ChkAcceptEula_Changed"/>
                        
                        <StackPanel Grid.Column="1">
                            <TextBlock Text="I accept the Minecraft End User License Agreement"
                                       FontWeight="Medium"
                                       FontSize="13"
                                       Margin="0,0,0,4"/>
                            <TextBlock FontSize="12"
                                       Opacity="0.8"
                                       Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                                       TextWrapping="Wrap">
                                <Run Text="PocketMC will write eula.txt automatically. Review "/>
                                <Hyperlink NavigateUri="https://aka.ms/MinecraftEULA"
                                           RequestNavigate="MinecraftEulaLink_RequestNavigate">Mojang's EULA</Hyperlink>
                                <Run Text=" before creating the instance."/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </StackPanel>
            </ui:Card>

            <Border x:Name="ProgressOverlay"
                    Visibility="Collapsed"
                    Width="{Binding ElementName=FormCard, Path=ActualWidth}"
                    Background="#10FFFFFF"
                    BorderBrush="{DynamicResource CardStrokeColorDefaultBrush}"
                    BorderThickness="1"
                    CornerRadius="14"
                    Padding="14"
                    HorizontalAlignment="Left"
                    Margin="0,16,0,14">
                <StackPanel>
                <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                    <ui:ProgressRing Width="18"
                                     Height="18"
                                     IsIndeterminate="True"
                                     Margin="0,0,8,0"/>
                    <TextBlock Text="Preparing your server files..."
                               VerticalAlignment="Center"
                               Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                               FontWeight="SemiBold"/>
                </StackPanel>
                <ProgressBar x:Name="PrgDownload"
                             Minimum="0"
                             Maximum="100"
                             Height="6"
                             Margin="0,0,0,8"/>
                <TextBlock x:Name="TxtProgress"
                           Text="Waiting for download..."
                           FontSize="12"
                           Foreground="{DynamicResource TextFillColorSecondaryBrush}"/>
                </StackPanel>
            </Border>
                </StackPanel>
            </ScrollViewer>
        </Grid>
        </DockPanel>
    </Grid>
</Page>
