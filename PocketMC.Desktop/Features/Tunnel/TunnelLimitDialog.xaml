<ui:FluentWindow x:Class="PocketMC.Desktop.Features.Tunnel.TunnelLimitDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="Tunnel Limit Reached"
        Width="520" Height="400"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        WindowCornerPreference="Round" ExtendsContentIntoTitleBar="True" Background="#1E1E2E">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="{DynamicResource TunnelLimitTitle}"
                   FontSize="18" FontWeight="Bold"
                   Foreground="#F38BA8"
                   Margin="0,0,0,12"/>

        <!-- Explanation -->
        <TextBlock Grid.Row="1"
                   Foreground="#BAC2DE" FontSize="13"
                   TextWrapping="Wrap"
                   Margin="0,0,0,16"
                   Text="{DynamicResource TunnelLimitMessage}"/>

        <!-- Existing tunnels list -->
        <Border Grid.Row="2" Background="#313244" CornerRadius="6" Padding="12" Margin="0,0,0,16">
            <StackPanel>
                <TextBlock Text="{DynamicResource TunnelLimitCurrentTunnelsLabel}"
                           Foreground="#CDD6F4" FontWeight="Bold" FontSize="13"
                           Margin="0,0,0,8"/>
                <ItemsControl x:Name="TunnelListControl" Foreground="#A6ADC8" FontSize="12">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,3">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="{Binding Port, StringFormat='Port {0}'}"
                                           Foreground="#F9E2AF" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding PublicAddress}"
                                           Foreground="#89B4FA"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </Border>

        <!-- Action Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button x:Name="OpenDashboardButton"
                    Content="{DynamicResource OpenTunnelDashboardButton}"
                    Width="180" Height="32"
                    Background="#89B4FA" Foreground="#1E1E2E"
                    FontWeight="Bold"
                    BorderThickness="0"
                    Margin="0,0,12,0"
                    Click="OpenDashboardButton_Click"/>

            <Button x:Name="ChangePortButton"
                    Content="{DynamicResource ChangePortButton}"
                    Width="120" Height="32"
                    Background="#45475A" Foreground="#CDD6F4"
                    BorderThickness="0"
                    Click="ChangePortButton_Click"/>
        </StackPanel>
    </Grid>
</ui:FluentWindow>
